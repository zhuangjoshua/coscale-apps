# Sheetsmile — static marketing site (Takyon Stage 3 output)

Complete self-contained website: index.html + pricing/faq/blog/privacy/terms,
styles.css + pages.css, assets/ (4 JPEGs). No build step, no JS, no backend.

- View: open index.html, or serve the folder: `python3 -m http.server 8000`
- Pricing shows the literal `$00` placeholder by design (no plan exists yet);
  real prices are hydrated at conversion from Takyon plan truth.
- This folder is a DISPOSABLE COPY. Canonical truth lives in the Takyon store
  (business `sheetsmile`, workspace rev 66). Do not hand-edit this copy with
  intent to keep changes — edits here are invisible to the pipeline and will be
  lost on re-materialization. Route changes through the Takyon tool rail.
